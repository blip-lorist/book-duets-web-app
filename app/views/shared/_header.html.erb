<nav class="nav navbar navbar-fixed-top">
  <div class="container-fluid">
   <!-- Collect the nav links, forms, and other content for toggling -->
   <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav navbar-right">


      <li class="dropdown">
          <% if session[:level] == nil || session[:level] == "safe"%>
            <a href="#" class="dropdown-toggle btn-success" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Naugtiness: SAFE<span class="caret"></span></a>
          <% elsif session[:level] == "edgy" %>
            <a href="#" class="dropdown-toggle btn-warning" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Naugtiness: <%= session[:level].upcase %><span class="caret"></span></a>
          <% elsif session[:level] == "filthy" %>
            <a href="#" class="dropdown-toggle btn-danger" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Naugtiness: <%= session[:level].upcase %><span class="caret"></span></a>
          <% end %>

          <ul class="dropdown-menu">
            <li><%= link_to "Safe", filter_path("safe"), method: :put %></li>
            <li><%= link_to "Edgy", filter_path("edgy"), method: :put %></li>
            <li><%= link_to "Filthy", filter_path("filthy"), method: :put %></li>
            <li role="separator" class="divider"></li>
          </ul>
      </li>
      <li><%= link_to "Newest Book Duets", book_duets_path %></li>
      <% unless current_user %>
      <li><%= link_to "Register / Log In", account_path %></li>
      <% end %>
      <% if current_user %>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hey, <%= current_user.username %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Mixtapes", mixtapes_path %></li>
              <li><%= link_to "Logout", logout_path %></li>
            </ul>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<div class="page-header">
  <h1><%= link_to "Book Duets", root_path%>    <small>Musician &amp; author Markov mashups</small></h1>
</div>
